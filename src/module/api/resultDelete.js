/* eslint-disable strict */
'use strict';

import { getDataStorage } from "../localStorage.js";
import updateData from './updateData.js';

const resultDelete = (form, body, data) => {

  updateData(form);



};

export default resultDelete;
